//- extends base.pug

//- //- This adds extra styles and js to already defined in base.pug
//- append main_vars
//-   - css_main.push.apply(css_main, ["css/inventory.css"])
//-   - js_main.push.apply(js_main, ["js/inventory.js"])
  
//- block content
//-   h1 Admin Panel
//-   table
//-     tr
//-       th Title
//-       th Price
//-       th Actions
//-     each product in products
//-       tr
//-         td= product.title
//-         td= product.price
//-         td 
//-           button(onclick=`editProduct(${product.id})`) Edit
//-           button(onclick=`deleteProduct(${product.id})`) Delete